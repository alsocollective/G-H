<!doctype html>
<!--[if lt IE 7]><html class="no-js lt-ie9 lt-ie8 lt-ie7" lang="en"> <![endif]-->
<!--[if IE 7]><html class="no-js lt-ie9 lt-ie8" lang="en"> <![endif]-->
<!--[if IE 8]><html class="no-js lt-ie9" lang="en"> <![endif]-->
<!--[if IE 9 ]><html class="ie9 no-js"> <![endif]-->
<!--[if (gt IE 9)|!(IE)]><!--> <html class="no-js"> <!--<![endif]-->  
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
	<link rel="stylesheet" type="text/css" href="//cloud.typography.com/717268/608946/css/fonts.css">

	{% if template == 'index' %}
		<title>{{ shop.name }} | {{ page_title }}</title>
	{% elsif template == '404' %}
		<title>Page Not Found | {{ shop.name }}</title>
	{% else %}
		<title>{{ page_title }} | {{ shop.name }}</title>
	{% endif %}
	{% if page_description %}
		<meta name="description" content="{{ page_description }}" />
	{% endif %}
	
	<link rel="canonical" href="{{ canonical_url }}" />
	<meta name="viewport" content="width=device-width" />  
	
	{{ content_for_header }}
	{{ 'style.min.css' | asset_url | stylesheet_tag }}
	{{ 'modernizr.js'  | asset_url | script_tag }}
	{{ '//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js' | script_tag }}

</head>
<body id="{{ page_title | handle }}" class="template-{{ template | replace: '.', ' ' | truncatewords: 1, '' | handle }}" >
	
	<div id="nav">
		<div>
			<div id="nav-logo">
				<!-- {{ 'logo.png' | asset_url | img_tag: shop.name }} -->
			</div>
			<ul id="nav-main">
			{% for link in linklists.main-menu.links %}

			{%comment%}		this figures out if there is a sub menue	{%endcomment%}
			{% assign has_drop_down = false %}
			{% assign child_list_handle = link.title | handle %}
			{% if linklists[child_list_handle] and linklists[child_list_handle].links.size > 0 %}
				{% assign has_drop_down = true %}
			{% endif %}

				<li class="{% if link.active %}selected{% endif %} {% if has_drop_down %}hassublinks{% endif %}">
					<span class="nav-item-clickable">
						<a href="{{ link.url }}">{{ link.title }}</a>
					</span>
					{% if has_drop_down %}
						<span class="sublist">
							<ul>
								{% for sublink in linklists[child_list_handle].links %}
									<li><a href="{{ sublink.url }}">{{sublink.title}}</a></li>	
								{% endfor %}
							</ul>
						</span>
					{% endif %}
				</li>
				
			{% endfor %}					
			</ul>
			<ul id="nav-cart" {% if cart.item_count == -1 %} style="display:none;"{%endif%}>
				<li>Knapsack</li>
				<li>{{ cart.item_count}} {{ cart.item_count | pluralize: 'item', 'items' }}</li>
				<li>{{ cart.total_price | money  }}</li>
			</ul>
		</div>
	</div>

	<div id="wrapper">
	{{ content_for_layout }}
	</div>

	<div class="footer">
		<div class="logo">
			{% include 'logo-small.html' %}
		</div><div class="footer-nav">
			{% for link in linklists.footer.links %}
				<li><a href="{{link.url}}#{{link.handle}}">{{link.title}}</a></li>
			{% endfor %}
		</div><div class="footer-nav">
			<ul>
				<li><a href="/contact">Contact Info</a></li>
				<li><a href="">hello@goodandhec.com</a></li>
			</ul>
		</div><div class="news">
			<input type="text">
		</div>

		<div class="notes">
			<h3>Global</h3>
			<p>
				Type! <br>
			</p>
			<h3>Product page</h3>
			<p>
				related products are done by what collection they are in, the most recent collection they where added is the related products.
			</p>
			<h3>collections</h3>
			<p>
				collections are based of product types <br>
				if the products have the same name only one is shown (this first) <br>
				Tags describe the colours, the products should be added alphabetically (colour wise) <br>
				point to having a buy tag in on the collection page, they need to select their size before they can buy the item
			</p>
		</div>
	</div>
	{{ 'main.min.js'  | asset_url | script_tag }}
</body>
</html>